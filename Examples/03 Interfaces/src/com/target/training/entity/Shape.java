package com.target.training.entity;

public interface Shape {
    // variables in an interface become public + final + static
    double PI = 3.14156;

    // functions in an interface become public + abstract
    void printArea();
}
